apiVersion: v1
kind: Service
metadata:
  name: booking-engine-api
spec:
  selector:
    app.kubernetes.io/name: booking-engine
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
